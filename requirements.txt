# Core dependencies: Used for C++ code generation (MiniModel DSL)
# build.sh and generate_model.py strongly depend on this library
sympy>=1.10

# Tool dependencies: Used for result visualization (tools/plot_trajectory.py)
# Contains data processing and plotting functionality
numpy>=1.20.0
pandas>=1.3.0
matplotlib>=3.4.0

# Plan to add unit tests to the Python part in the future
# pytest